<div class="col-xl-8 col-xl-offset-2 col-xs-12">
    <div class="row">
        <div class="text-center col-md-10 col-md-offset-1">
            {{#ifCond result.orderStatus '==' 'Paid'}}
                <h2 class="text-success">{{ @root.__ "Płatność przeprowadzono pomyślnie" }}</h2>
            {{else}}
                <h2 class="text-danger">{{ @root.__ "NIe udało się przeprowadzić pomyślnie, spróbuj ponownie." }}</h2>
            {{/ifCond}}
            {{#if result}}
                <div>
                    <p><strong>{{ @root.__ "Order ID" }}:</strong> {{result._id}}</p>
                    <p><strong>{{ @root.__ "Payment ID" }}:</strong> {{result.orderPaymentId}}</p>
                </div>
            {{/if}} 
            {{#ifCond result.orderStatus '==' 'Paid'}}
                <h3 class="text-warning">{{ @root.__ "Proszę zachować szczegóły płatności." }}</h3>
            {{/ifCond}}
            <a href="/" class="btn btn-warning">Home</a>
        </div>
    </div>
</div>
